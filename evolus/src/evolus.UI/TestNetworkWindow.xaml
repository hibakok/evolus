<Window x:Class="evolus.UI.TestNetworkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Тестирование нейросети" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите входные данные (через пробел):" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="1">
            <TextBox x:Name="TxtInput" Height="80" AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>
            <Button Content="Выполнить" Click="BtnExecute_Click" Height="35"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Результат:" Margin="0,15,0,5"/>
        <TextBox x:Name="TxtOutput" Height="80" IsReadOnly="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" Background="#F0F0F0"/>

        <Button Grid.Row="3" Content="Закрыть" Width="80" Height="30" 
                HorizontalAlignment="Right" Margin="0,15,0,0" Click="BtnClose_Click"/>
    </Grid>
</Window>
